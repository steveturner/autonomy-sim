# Optional. Without this key, 3D mode uses the normal Cesium globe.
VITE_GOOGLE_MAPS_API_KEY=

# Optional when the state server is not on the default port.
# VITE_WS_URL=ws://127.0.0.1:9000/api/v1/stream

